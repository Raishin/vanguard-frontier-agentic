# Packet B result

Key Vault sources confirm RBAC over legacy access policies for critical workloads, managed identities, soft delete, purge protection, rotation/renewal, Event Grid monitoring, certificate issuer policy/exportability concerns, and vault segmentation. AKS sources confirm rolling upgrade surge/drain/soak behavior, max unavailable disruption risk, PDB sensitivity, and blue-green rollback windows. Documentation does not prove cluster or vault state.
