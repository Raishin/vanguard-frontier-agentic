# Packet B result

Cost Management docs confirm budget alerts notify but do not stop consumption, cost data latency, action group integration, and FinOps budgeting patterns. Azure RBAC docs confirm least-privilege scope, privileged administrator role risk, PIM preference, role assignment alerts, and propagation delay caveats. Key Vault docs confirm soft delete, purge protection irreversibility, retention windows, purge role risk, and key rotation policy behavior.
