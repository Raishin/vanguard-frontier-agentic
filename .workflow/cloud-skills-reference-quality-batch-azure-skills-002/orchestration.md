# Orchestration

Run five isolated item passes in sorted order. For each pass: inspect local files, gather Microsoft Learn evidence, compare, patch minimal references/metadata/catalog, then write a result note. After all five, regenerate skill manifest and asset integrity, run narrow validators, and report without committing.
