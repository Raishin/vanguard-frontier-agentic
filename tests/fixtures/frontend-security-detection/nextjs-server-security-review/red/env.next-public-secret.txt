# .env.production
# Someone prefixed a real secret with NEXT_PUBLIC_ so a client component could
# read it — this inlines the secret into the JavaScript bundle at build time,
# exposing it to anyone who views page source.
NEXT_PUBLIC_API_KEY=__REDACTED_EXAMPLE_SECRET_not_a_real_key__
NEXT_PUBLIC_ANALYTICS_ID=abcdefghijk
