# .env.production
# The secret is unprefixed, so Next.js keeps it server-only — accessible from
# Server Actions and Route Handlers via process.env.API_KEY, never inlined
# into the client bundle. Only the genuinely public analytics ID is exposed.
API_KEY=__REDACTED_EXAMPLE_SECRET_not_a_real_key__
NEXT_PUBLIC_ANALYTICS_ID=abcdefghijk
