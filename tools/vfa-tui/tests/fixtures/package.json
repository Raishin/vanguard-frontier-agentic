{
  "name": "@raishin/vanguard-frontier-agentic",
  "version": "2.0.0",
  "scripts": {
    "validate:catalog": "python3 tests/validate-catalog.py",
    "validate:links": "python3 tests/validate-links.py --offline",
    "validate:schema": "python3 tests/validate-skill-frontmatter-schema.py",
    "validate": "npm run validate:catalog && npm run validate:links && npm run validate:schema"
  }
}
