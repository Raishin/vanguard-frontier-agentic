//! Policy module — parsing, evaluation, trust boundaries, lifecycle, and violations.
//! Module skeleton — implemented in later waves.

pub mod engine;
pub mod lifecycle;
pub mod parser;
pub mod trust;
pub mod violations;
